<div>
  <% @chatrooms.each do |chatroom| %>
    <%= link_to chatroom_path(chatroom), class: "block p-4 bg-gray-100 rounded-lg mb-2 cursor-pointer" do %>
      <h3 class="text-lg font-semibold"><%= chatroom.name %></h3>
    <% end %>
  <% end %>
</div>
