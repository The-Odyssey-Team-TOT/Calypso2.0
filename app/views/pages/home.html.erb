<div id="image-container" class="min-h-screen bg-black text-white flex items-center justify-center">
  <div id="content" class="text-center">
    <h1 class="text-6xl font-bold mb-4">Calypso</h1>
    <p class="text-xl mb-8">Your conversations are safe.</p>
    <div class="space-x-4 home-btn">
      <%# <%= link_to 'Create', new_chat_room_path, class: 'bg-gray-300 hover:bg-gray-400 text-black font-bold py-2 px-4 rounded' %>
      <%# <%= link_to 'Join', chat_rooms_path, class: 'bg-gray-300 hover:bg-gray-400 text-black font-bold py-2 px-4 rounded' %>
      <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn bg-gray-300 hover:bg-gray-400 text-white font-bold py-2 px-4 rounded' %>
      <%= button_to 'Sign In', new_user_session_path, class: 'btn bg-gray-300 hover:bg-gray-400 text-white font-bold py-2 px-4 rounded' %>
    </div>
  </div>
  <%= link_to "sign in", new_user_session_path %>
</div>
